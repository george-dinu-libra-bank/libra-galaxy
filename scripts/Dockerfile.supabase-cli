FROM node:22-alpine

RUN apk add --no-cache docker-cli \
    && npm install --prefix /opt/supabase supabase@2.115.0

ENV PATH="/opt/supabase/node_modules/.bin:${PATH}"

WORKDIR /workspace

ENTRYPOINT ["supabase"]
