FROM node:22-alpine

WORKDIR /app

# Dependintele se instaleaza in imagine; node_modules e montat ca volum anonim
COPY package.json package-lock.json* ./
RUN npm install

COPY . .

EXPOSE 3000
CMD ["npm", "run", "dev", "--", "-H", "0.0.0.0", "-p", "3000"]
