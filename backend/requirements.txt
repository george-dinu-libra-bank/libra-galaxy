fastapi>=0.115
uvicorn[standard]>=0.34
pydantic>=2.10
pydantic-settings>=2.7
python-multipart>=0.0.20
python-dotenv>=1.0

supabase>=2.11
# [crypto] aduce `cryptography`, necesar pentru verificarea ES256 (JWKS) —
# proiectele Supabase noi semneaza token-uri asimetric, nu cu secret HS256.
PyJWT[crypto]>=2.10

openai>=1.59
httpx>=0.28
tiktoken>=0.8

pypdf>=5.1
PyYAML>=6.0

# --- Verificare identitate (OCR buletin + DeepFace selfie) -------------------
# Pinuri exacte, nu >= : deepface/tf-keras sunt sensibile la versiune si la
# Python 3.11 (Dockerfile.dev), spre deosebire de restul dependintelor de mai sus.
deepface==0.0.93
tf-keras==2.18.0
pytesseract==0.3.13
pillow==11.1.0
opencv-python-headless==4.10.0.84

pytest>=8.3
pytest-asyncio>=0.25

# --- Financial advisor (Cristi) — bucla de tool-calling peste Azure AI Foundry,
# cu AnalizaService (repos card/cont/tranzactie) si detectorul de neregularitati.
azure-ai-inference>=1.0.0b9
azure-identity>=1.19,<2
anyio>=4.0
joblib>=1.4,<2
scikit-learn>=1.5,<2
