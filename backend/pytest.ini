[pytest]
pythonpath = .
asyncio_mode = auto
markers =
    llm_eval: cheama Foundry-ul real (nu un dublu) — nu ruleaza in suita rapida, doar manual cu `pytest -m llm_eval`.
addopts = -m "not llm_eval"
